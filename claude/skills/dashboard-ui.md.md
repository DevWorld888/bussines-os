# Dashboard UI Skill

Use this skill when building dashboard pages.

Rules:
- Follow design.md exactly.
- Use monochrome colors.
- Use cards, tables and clean spacing.
- Use shadcn/ui if installed.
- Keep components reusable.
- Avoid visual noise.
- Do not invent random colors.
- Use fake data only if database is not ready.